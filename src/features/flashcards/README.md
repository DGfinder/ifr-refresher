# Flashcards Feature

Owns the flashcard session UI over drill questions. Components should receive queues/actions from feature screens/hooks rather than reaching into global content directly where practical.
