// pillars-lint-disable-file -- metadata/manifest hex values must be emitted as literal web colour strings.
export const IFR_THEME = {
  lightBackground: "#ffffff",
  lightTheme: "#12263e",
  darkTheme: "#101c2b",
} as const;
